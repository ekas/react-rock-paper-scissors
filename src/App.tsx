import { BrowserRouter as Router, Route, Routes } from "react-router-dom";
import "./App.less";

function App() {
  <Router>
    <div className="App">
      <main className="container">
        <Routes>
          <Route path="/" element={<></>} />
        </Routes>
      </main>
    </div>
  </Router>;
}

export default App;
